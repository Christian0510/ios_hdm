import 'package:flutter/material.dart';

class TodusPage extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('toDus'),
      ),
    );
    
  }
}